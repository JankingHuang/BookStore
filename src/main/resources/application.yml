server:
  port: 8888

spring:
  profiles:
    active: jdbc
  jackson:
    date-format: yyyy-MM-dd HH:mm:ss
    time-zone: GMT+8

mybatis:
  mapper-locations: classpath:mapping/*Mapper.xml
  type-aliases-package: com.asxb.bookstore.pojo
#  configuration:
#    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl # 打印sql语句

